# Optional MSCQR local print agent overrides.
# Copy values into the platform-specific agent.env created by the installer.

# Local listener
PRINT_AGENT_HOST=127.0.0.1
PRINT_AGENT_PORT=17866

# Optional outbound site gateway settings for NETWORK_IPP printers.
# PRINT_GATEWAY_BACKEND_URL=https://mscqr.example.com/api
# PRINT_GATEWAY_ID=gw_1234567890
# PRINT_GATEWAY_SECRET=replace-with-bootstrap-secret
