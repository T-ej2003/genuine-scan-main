name: Authorize component installation identity bootstrap
run-name: Authorize initial component identity bootstrap ${{ inputs.transition_id }}

'on':
  workflow_dispatch:
    inputs:
      source_sha: {description: Exact current protected-main SHA, required: true, type: string}
      transition_id: {description: Unique first-bootstrap transition UUID, required: true, type: string}

permissions:
  contents: read
  actions: read

concurrency:
  group: component-installation-identity-bootstrap-authorization
  cancel-in-progress: false

jobs:
  authorize:
    if: github.repository == 'T-ej2003/genuine-scan-main' && github.ref == 'refs/heads/main' && github.run_attempt == 1
    runs-on: ubuntu-latest
    timeout-minutes: 15
    environment: production-component-installation-identity-bootstrap
    steps:
      - uses: actions/checkout@v6
        with: {ref: '${{ github.sha }}', fetch-depth: 0, persist-credentials: false}
      - uses: actions/setup-node@v6
        with: {node-version: 24}
      - run: npm ci --ignore-scripts --no-audit --no-fund
      - name: Bind explicit approval to the exact initial identities and broker package
        env:
          GH_TOKEN: ${{ github.token }}
          SOURCE_SHA: ${{ inputs.source_sha }}
          TRANSITION_ID: ${{ inputs.transition_id }}
        run: node scripts/aws/component-identity-bootstrap-authorization.mjs prepare
      - uses: actions/upload-artifact@v7
        with:
          name: component-identity-bootstrap-authorization
          path: ${{ runner.temp }}/component-identity-bootstrap-authorization.json
          if-no-files-found: error
          retention-days: 90
          # Initial execution authenticates this run and archive, then stores
          # its non-secret authorization durably in the fixed AWS journal.
          # This workflow has no OIDC permission or AWS mutation step.
