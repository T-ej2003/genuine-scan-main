# MSCQR launch handoff

The historical pre-refresh root-drop instance is authenticated with `identity_schema_version = 0` and `identity = null`; the persisted refresh must replace that null with the exact provider identity documented below.

This is a planning handoff, not authorization evidence. It was generated while repairing the release-deployer KMS capability. The next execution must resolve a fresh merged `origin/main` SHA and regenerate every source-bound artifact.

The complete launch producer/consumer graph is in `documents/ops/MSCQR_PRODUCTION_LAUNCH_HANDOFF_MANIFEST-v1.json`. Its hard pre-mutation gate includes the temporary Stage-A KMS creation-capability lifecycle: authorization, exact saved-plan apply, root-drop ownership verification, revocation, and independent absence verification must complete before the governed ECS cutover.

Operator-owned boundaries are explicit: approval metadata, a fresh rotation identifier, the root-only signing session, and MFA/verifier actions. No password, secret value, private key, session credential, or MFA value is an artifact. The next run must discard the failed serial-42/43 binary plan as executable input and all other stale executable artifacts, but retain the recorded failed-apply provenance bindings (source, transition, plan SHA, state identity, and CreateKey evidence) required to authenticate the exact existing 1/0 key. It must authorize the exact temporary recovery capability before provider refresh, read Stage-A state afresh, classify a new non-targeted recovery plan, and apply only that new plan once its exact mutation envelope is reviewed.

The authorization-enabled provider refresh is a persisted refresh-only transition, not an AWS resource mutation. If the exact authenticated already-refreshed 1/0 key instance is marked `tainted`, the canonical runner first removes only that marker with `terraform untaint`; it requires the same lineage/backend/workspace/key/provider identity, serial +1, and no other semantic state change. A saved `terraform plan -refresh-only` is then classified, byte-revalidated, applied once to persist only authenticated provider-computed state, and pulled again. For the historical 1/0 topology, lineage, backend, workspace, exact key properties, and 1/0 topology remain unchanged; the provider may advance the Terraform serial/state SHA only when it populates the exact root-drop ARN/key ID, the exact AWS provider v6.56.0 instance identity `{ account_id: "368992683803", id: <authenticated-key-id>, region: "eu-west-2" }`, and, when present, advances only the exact managed `aws_db_instance.green.latest_restorable_time` provider-computed RFC3339 UTC value. Missing, substituted, or extended root-drop identity metadata fails closed. That RDS allowance requires an update without replacement or unknown values, a strictly forward timestamp, no other RDS attribute difference, and zero actionable `resource_changes`; every other drift remains fail closed. Fresh recovery and zero-drift plans independently classify `resource_drift` and permit only the same contracted RDS timestamp observation. Current Stage-A state SHA values use identity version 2 semantic canonicalization: only uniquely keyed top-level `check_results` ordering is normalized; security-relevant resource, attribute, dependency, output, and nested-array changes remain identity changes. Authenticated state bytes pass fatal UTF-8 decoding and the precision-preserving numeric parser before JSON parsing; malformed UTF-8 and round-trip-colliding numeric literals fail closed, while equivalent spellings such as `1`, `1.0`, and `1e0` share one identity. The post-transition identity is used for the new plan and all later checks. These boundaries perform `AWS_RESOURCE_MUTATIONS=0`; each executed untaint or refresh-only apply is accounted as its own Terraform state write. Any other state change fails closed. No canonical producer was missing during discovery. The repository owns the Stage-A prerequisite/recovery, rotation, root evidence, runtime, inventory, broker proof, cutover, convergence, verification, and onboarding transitions named in the manifest.
