{
  "schemaVersion": 3,
  "service": "MSCQR",
  "lastAudited": "2026-05-15",
  "asgStatus": "CONDITIONALLY_READY",
  "statusReason": "Regional Redis, regional S3 object storage, ASG web-node worker exclusion, SSM-backed bootstrap, self-sufficient launch-template UserData for plain Ubuntu hosts, optional ASG SSH KeyName support, explicit ASG web instance profile input, explicit ASG public-IP networking mode, and the rolling deploy contract are committed. The remaining go/no-go is a first live ASG create/attach plus replacement-instance drill with no production DNS cutover.",
  "safeToCreateAsg": false,
  "safeToModifyProductionDns": false,
  "safeToMutateRdsData": false,
  "proofs": [
    {
      "id": "REDIS_SHARED_ENDPOINT_PROVEN",
      "status": "proven",
      "area": "redis-sessions-rate-limits-cache-leases",
      "evidence": [
        "Operator evidence: Mumbai and Cape Town have regional ElastiCache/Valkey.",
        "Operator evidence: EC2 instances can reach regional Redis.",
        "Operator evidence: backend and worker use REDIS_URL=rediss://regional-elasticache:6379/0 and REDIS_TLS=true.",
        "Operator evidence: /api/health/ready reports redis configured=true ready=true in both regions.",
        ".env.production.mumbai.example and .env.production.capetown.example now document rediss:// regional Redis and REDIS_TLS=true."
      ],
      "condition": "ASG web nodes must use the same regional REDIS_URL and REDIS_TLS=true. The local Redis service in docker-compose.yml is retained only for legacy/local-state Compose paths."
    },
    {
      "id": "OBJECT_STORAGE_SHARED_PROVEN",
      "status": "proven",
      "area": "object-storage",
      "evidence": [
        "Operator evidence: regional object storage uses S3/default credentials with endpoint=null and mode=default-credentials.",
        "Operator evidence: OBJECT_STORAGE_ENDPOINT, OBJECT_STORAGE_ACCESS_KEY, and OBJECT_STORAGE_SECRET_KEY are empty in both regional env files.",
        ".env.production.mumbai.example and .env.production.capetown.example now keep static object storage endpoint and credentials empty.",
        "docker-compose.asg-web.yml has no MinIO service."
      ],
      "condition": "ASG web nodes must use regional S3/default credentials. The local MinIO service in docker-compose.yml is retained only for legacy/local-state Compose paths."
    },
    {
      "id": "WORKER_TOPOLOGY_CONDITIONALLY_PROVEN",
      "status": "conditional",
      "area": "background-workers",
      "evidence": [
        "docker-compose.yml keeps the worker behind profiles: [\"worker\"].",
        "docker-compose.asg-web.yml contains backend and frontend only, with no worker service.",
        "Both backend Compose modes force RUN_BACKGROUND_WORKERS=false for HTTP backend processes.",
        "Existing standalone regional EC2 worker ownership is explicit: docker compose --profile worker up -d --build backend worker frontend.",
        "Operator evidence: current worker containers run with RUN_BACKGROUND_WORKERS=true.",
        "Operator evidence: COMPLIANCE_PACK_SCHEDULER_ENABLED=false in both regions."
      ],
      "condition": "Exactly one worker container per region. ASG web nodes must not start worker containers. COMPLIANCE_PACK_SCHEDULER_ENABLED must stay false unless separately approved. Worker horizontal scaling is not assumed safe."
    },
    {
      "id": "ASG_SECRET_BOOTSTRAP_CONDITIONALLY_PROVEN",
      "status": "conditional",
      "area": "secrets-bootstrap",
      "evidence": [
        "scripts/dr/bootstrap-asg-web-node.sh fetches ASG web env values from SSM Parameter Store using --with-decryption.",
        "documents/ops/aws-asg-web-ssm-parameter-manifest.json defines root-env and backend-env parameter names only, with no values.",
        "The bootstrap script writes project .env and backend/.env with 0600 permissions and does not print secret values.",
        "The bootstrap script forces RUN_BACKGROUND_WORKERS=false, RUN_DB_MIGRATIONS_ON_START=false, COMPLIANCE_PACK_SCHEDULER_ENABLED=false, REDIS_TLS=true, empty object storage endpoint/static credentials, and OBJECT_STORAGE_FORCE_PATH_STYLE=false.",
        "The bootstrap script starts docker-compose.asg-web.yml backend and frontend only, then verifies /healthz and /api/health/ready through Nginx.",
        "scripts/dr/generate-asg-apply-plan.sh and scripts/dr/apply-asg-launch-template-approved.sh require ASG_WEB_INSTANCE_PROFILE_ARN or ASG_WEB_INSTANCE_PROFILE_NAME and do not reuse the source instance profile automatically.",
        "Generated launch template data includes base64 UserData that runs scripts/dr/bootstrap-asg-web-node.sh with the target region group and AWS region.",
        "scripts/dr/generate-asg-apply-plan.sh and scripts/dr/apply-asg-launch-template-approved.sh support ASG_ASSOCIATE_PUBLIC_IP=true or false only.",
        "scripts/dr/generate-asg-apply-plan.sh and scripts/dr/apply-asg-launch-template-approved.sh support optional ASG_KEY_NAME and omit KeyName when it is not set.",
        "scripts/dr/generate-asg-apply-plan.sh and scripts/dr/apply-asg-launch-template-approved.sh support ASG_REPO_URL, ASG_REPO_BRANCH, and ASG_REPO_DIR.",
        "When ASG_ASSOCIATE_PUBLIC_IP=true, launch template data uses NetworkInterfaces[0].AssociatePublicIpAddress=true with Groups and no top-level SecurityGroupIds.",
        "When ASG_ASSOCIATE_PUBLIC_IP=false, launch template data uses top-level SecurityGroupIds and no NetworkInterfaces.",
        "UserData installs/checks git, Docker, and docker compose before repository checkout.",
        "UserData clones ASG_REPO_URL into ASG_REPO_DIR when the checkout is missing, or fetches/resets ASG_REPO_BRANCH when the checkout already exists.",
        "UserData writes /var/log/mscqr-asg-bootstrap.log and mirrors non-secret status/failure lines to cloud-init console output.",
        "ops/aws/iam/dr/asg-web-instance-profile-policy.template.json defines least-privilege SSM read, scoped KMS decrypt, regional S3 artifact access, and ECR image pull permissions."
      ],
      "condition": "ASG launch templates must call scripts/dr/bootstrap-asg-web-node.sh with the approved region group and AWS region through UserData. The explicit ASG web instance profile must be scoped to the documented SSM prefix and artifact bucket."
    },
    {
      "id": "ROLLING_DEPLOY_POLICY_CONDITIONALLY_PROVEN",
      "status": "conditional",
      "area": "rolling-deploy",
      "evidence": [
        "documents/ops/aws-asg-rolling-deploy-policy.md defines the ASG rolling deploy contract, smoke tests, rollback rules, and replacement-instance drill.",
        "documents/ops/aws-asg-rolling-deploy-policy.checklist.json commits concrete values for deregistration delay, health grace, warmup, min healthy percentage, initial capacity, target healthy count, and rollback alarm placeholders.",
        "scripts/dr/generate-asg-apply-plan.sh reads the rolling policy checklist and includes those values in the generated ASG apply plan.",
        "scripts/dr/generate-asg-apply-plan.sh validates proposed launch template data for IamInstanceProfile, UserData, required IMDSv2, security group, AMI, and instance type.",
        "scripts/dr/apply-asg-launch-template-approved.sh reads the rolling policy checklist, refuses non-policy first-rollout capacity, and refuses apply when deregistration delay or rollback alarm names are unsafe.",
        "scripts/dr/apply-asg-launch-template-approved.sh validates launch-template-data.json for IamInstanceProfile, UserData, required IMDSv2, AMI, instance type, KeyName behavior, and the expected public-IP networking shape before any create/update path.",
        "scripts/dr/apply-asg-launch-template-approved.sh writes intended and live launch template data artifacts, creates a fresh launch template version when data differs, and verifies the ASG points at the intended version before capacity update."
      ],
      "condition": "Before a real regional apply, operators must supply the reviewed CloudWatch rollback alarm names and complete one no-DNS ASG create/attach plus replacement-instance drill with evidence."
    }
  ],
  "blockers": [],
  "conditionsBeforeReaudit": [
    "Production DNS remains rolled back to London EC2 until a separate approved DNS cutover.",
    "ASG web nodes use docker-compose.asg-web.yml, not the legacy local-state Compose path.",
    "Redis is external/shared per region and not container-local per EC2 node.",
    "Object storage is shared regional S3/default credentials and not container-local MinIO per EC2 node.",
    "Migrations are run as an explicit one-shot release step, not by every booting instance.",
    "Web nodes run RUN_BACKGROUND_WORKERS=false.",
    "One singleton worker container runs per region outside the ASG web node path.",
    "COMPLIANCE_PACK_SCHEDULER_ENABLED remains false unless separately approved.",
    "Secrets are injected through IAM-authenticated SSM Parameter Store bootstrap with no secret values in logs.",
    "Fresh instance bootstrap is repeatable from image, user data, env, and repository scripts.",
    "Launch template plan and apply require ASG_WEB_INSTANCE_PROFILE_ARN or ASG_WEB_INSTANCE_PROFILE_NAME.",
    "Launch template plan and apply include deterministic base64 UserData for ASG web-node bootstrap.",
    "Launch template plan and apply use ASG_ASSOCIATE_PUBLIC_IP=true only as a temporary retry bridge for public subnets without MapPublicIpOnLaunch; the preferred steady-state design is private ASG subnets with NAT Gateway or VPC endpoints and ASG_ASSOCIATE_PUBLIC_IP=false.",
    "Launch template plan and apply support optional ASG_KEY_NAME for debug retries; Mumbai no-DNS debug retry should use ASG_KEY_NAME=mscqr-prod-mumbai.",
    "Launch template plan and apply require ASG_REPO_URL and support ASG_REPO_BRANCH=main and ASG_REPO_DIR=/home/ubuntu/genuine-scan-main so a plain Ubuntu 22.04 host can install prerequisites and clone the app before SSM env bootstrap.",
    "ALB health checks use /healthz for shallow liveness or /api/health/ready for dependency readiness according to the rollout phase.",
    "Rolling deploy and rollback behavior are documented in-repo and must be validated through the first no-DNS regional replacement-instance drill."
  ],
  "requiredValidationCommands": [
    "node scripts/dr/check-asg-multi-instance-readiness.mjs",
    "npm run check:aws-dr-safety",
    "npm run verify:guardrails",
    "npm run check:documents",
    "git diff --check"
  ],
  "operatorInputsRequiredForConditionalReady": [
    "Provide the reviewed CloudWatch rollback alarm names for the selected region before running apply-asg-launch-template-approved.",
    "Run one controlled regional ASG create/attach and replacement-instance drill with no production DNS cutover, then capture target-health and CloudWatch evidence."
  ]
}
