# B01 prerequisite to normal backend deployment bridge

This bridge preserves the immutable RLS origin `0f7ae1a70eec588ef4fdcb2b53e9f42e831c4414`, bridge merge `e1c16977e9fdce7a19dc267ba3c816ef4ff14597`, live-predecessor correction `69e71ab21c847d27f8a76795a6de6f623313c8ad`, read-only runtime-evidence merge `a2bed229ddfb893d4dcc53232ea5466d23d4e80a`, expired-task recovery merge `61b1eeceb5462e70830737fd5e697c2c55d321ab`, bounded terminal-event merge `d00a8783be458719597b8b7124ff70ac76119e4f`, and provenance correction `374feb5f8853d06f88af99ab632c03928e579495`. The deployment source must be the exact protected-main merge SHA of the narrow classification-diagnostic correction that immediately follows that complete reviewed lineage. Executable diff attestations bind every range and reject arbitrary descendants plus application, authentication, RLS, schema, MFA, session, invitation, business, or unclassified changes.

The local production operator runs the source-bound B01 prerequisite command from the exact protected-main checkout. Before task registration it requires the stable revision-19 service, its immutable task-definition identity and security-relevant runtime shape, and the backend image digest's unique source tag `945692f49c6d262b0a54b9b8e4240ef4c21688eb`. That Node runtime image contains the production dependencies used by the separately injected, fixed, and read-back executor command; live service environment, roles, and commands are not inherited. The executor accepts only the seven reviewed catalogue mutations, authenticates the complete relevant database predecessor before its first mutation, applies them in one serializable transaction, authenticates the successor, and emits a 30-minute receipt. A converged successor emits a zero-write receipt. Any other catalogue state fails before mutation.

The predecessor and successor identities cover every B01 policy's schema, table, command, permissiveness, roles, comment, and PostgreSQL 18 canonical `USING` and `WITH CHECK` deparse hashes. They also cover the two changed functions' signatures, bodies, owners, execution properties and ACLs; the owner/pre-auth role security attributes; and the relevant `AuditLogOutbox` ownership, table ACL, payload-column ACL, forced-RLS state, and role-switch capability. The compact policy-state file is generated from the canonical package on disposable PostgreSQL 18 and the certification test requires exact equality with a freshly collected catalogue.

The receipt binds both semantic SHAs, predecessor and successor identities, package and migration digests, executor bytes, the complete CloudTrail RunTask request, stopped ECS task evidence, production environment, result, timestamp, and bridge patch digest. CloudTrail must match the source-built cluster, task definition, launch type, count, execute-command setting, client token, network configuration, and contain no overrides or extra request fields except AWS's observed false `dryrun` and `enableECSManagedTags` defaults. DescribeTasks may contain only AWS's observed inert materialization: an empty override object, empty arrays, or one name-only override for the expected container. The existing Normal Production Deployment workflow accepts the receipt only while its deployment SHA is still current protected main. It re-reads the stopped executor task and task definition using the existing normal-deployer read permissions, then runs the existing backend image, approval, OIDC, ECS, rollback, health, and smoke implementation. The security classifier remains unchanged and frontend deployment is excluded.

The #567 database change is additive for revision 19: existing refresh operations retain their contracts; the new finalization operation, grant, and narrowly bound policy are unused by the old backend. Therefore applying RLS first is backward-compatible. Application rollback does not roll RLS backward.

After this bridge is merged, execution starts only from a clean checkout whose `HEAD` and `origin/main` are the exact bridge merge SHA. A separately authorized operator runs:

```sh
node scripts/aws/apply-production-b01-prerequisite.mjs \
  --deployment-source-sha "$(git rev-parse HEAD)" \
  --aws-profile <approved-root-profile> \
  --receipt-out <new-private-receipt-path>
```

The command fails before SQL writes unless revision 19 and the exact #567 RLS predecessor are live. Failures emit only an allowlisted stage and sanitized code; read-only classification assertion failures may additionally emit one closed, source-defined invariant identifier, while raw exceptions, catalogue values, and secrets are never logged. After it reports `APPLIED` or `ALREADY_CONVERGED`, the operator base64-encodes that new private receipt and dispatches `Normal Production Deployment` from the same still-current protected-main SHA with `baseline=false` and `b01_prerequisite_receipt_base64` set. The workflow requires the existing `production-normal-deploy` approval and rejects an expired receipt or any later main advance.

Do not rerun an ambiguous executor failure. After separate authorization, reconcile the durable catalogue first with the fixed source-owned read-only entry point:

```sh
node scripts/aws/probe-production-b01-prerequisite.mjs \
  --deployment-source-sha "$(git rev-parse HEAD)" \
  --ambiguous-task-arn <exact-failed-mutation-task-arn> \
  --ambiguous-deployment-source-sha <exact-failed-attempt-source-sha> \
  --aws-profile <approved-root-profile>
```

Before registering the probe, the entry point authenticates the exact failed mutation task, its historical source-built command, fixed CloudTrail launch evidence, and complete mutation-family launch history. A retained task must have exact terminal ECS state. An expired future task must instead have the exact durable ECS `STOPPED` task-state event captured by the cluster's native EventBridge-to-CloudWatch event capture. The one pre-capture historical attempt is allowlisted by exact task ARN, task-definition revision, source SHA, CloudTrail event ID, and timestamp; it is never generalized into `MISSING == STOPPED`. Complete paginated RUNNING and STOPPED-desired family censuses reject active siblings, retained unexpected siblings, and tasks transitioning toward STOPPED; each census scans both states twice because ECS tasks never have desired status `PENDING`. After catalogue classification, recovery requires a stable `launch history A -> census A -> launch history B -> census B -> launch history C` bracket. The trailing history authenticates the launch set after the final census, while each double census catches an active or fast-stopped launch that CloudTrail has not exposed yet.

The fixed read-only task supplies no overrides. Inside PostgreSQL, one `READ COMMITTED, READ ONLY` transaction waits with a bounded timeout on the exact transaction-scoped advisory lock used by the mutation executor, then establishes the catalogue snapshots used by the shared collector and classifier. It reports only `PREDECESSOR`, `SUCCESSOR`, `PARTIAL`, or `UNKNOWN` plus bounded identities/invariants and exits without a mutation path. A substituted/expired task without sufficient evidence, unaccounted later launch, active sibling executor, incomplete pagination, lock timeout, or synchronization failure yields no retry-safe catalogue classification.

## Expired-task evidence inventory and future retention

For failed task `a74666b43a80488c95746f4b7f789fb2`, CloudTrail retains one authoritative fixed `RunTask` event and ECS retains exact task definition revision 1. The executor CloudWatch stream retains one supporting bounded failure marker. `DescribeTasks` now returns `MISSING`; there is no `StopTask` event, EventBridge archive, ECS event-capture rule, CloudTrail trail, or CloudTrail Lake store proving its terminal transition. Recovery therefore requires the exact historical allowlist plus complete CloudTrail launch history, complete current family census, the shared database advisory lock, post-classification launch/census revalidation, and canonical catalogue classification.

Before any future mutation launch, the executor entry point requires Amazon ECS native event capture for the production cluster, with at least 30 days retention and exactly one enabled rule targeting the wildcard-free CloudWatch Logs group ARN for `/aws/events/ecs/containerinsights/mscqr-prod-euw2-main/performance`. The rule must either capture all `aws.ecs` events or explicitly capture `ECS Task State Change`; deployment-only or additionally constrained patterns fail closed. It refuses to launch without that durable capture and refuses to issue a receipt until the exact task's authenticated `STOPPED` event is present. Terminal-event scans are bounded from the authenticated launch through the actual collection observation, while pagination remains fail closed; delayed delivery is accepted only when the authenticated event timestamp remains inside that window. Event capture must be enabled as a separately authorized AWS control-plane prerequisite; this repository does not create a broker, database, Lambda, or state machine for it.
